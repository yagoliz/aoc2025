# AoC 2025

My attempt at solving all problems of this year's AoC.

The input is expected to be in the `input` folder. The file for each day should be in `day<N>.txt`.

To run all days:
```bash
uv run main.py
```

To benchmark performance, add the `--perf` flag:
```bash
uv run main.py --perf
```
